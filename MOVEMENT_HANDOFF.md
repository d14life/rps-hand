# Isolated movement prototype — Codex

Update 03 (cf3e41a): latest user correction is mouse-like horizontal INDEX SWIPE turning, measured from 2D line length and speed. This is now the default: SwipeController in swipe.mjs uses direct mirrored fingertip x, 0.3% frame-width jitter slack, speed-dependent gain, no dwell/smoothing/coasting, and fold/loss reanchoring. No depth estimation in this mode. Camera yaw changes while position stays fixed. Prior depth/strafe movement remains an optional 'Drag to move' mode, now relative to camera heading. Orange fingertip trail and swipe pixel/speed readout added. Eight swipe tests pass; real model reference-image pointer check passes. Browser demo: 158px -> approximately 48.2 degrees, X/Z unchanged. Actual phone camera feel remains unverified. Preserve /movement/ remote commits when pushing hand work.

Update 02 (a3ad084): user requested INDEX as the default mode, visible camera+landmark overlay, movable/collapsible controls and a clearer scene. app.mjs now sets index mode on startup. ui.mjs/ui.css provide draggable controls and preview, hide/show, layout reset, green skeleton/yellow index overlay, and improved crosshair/contrast. Phone-size drag/collapse/restore/default-mode checks passed. Pinch remains optional. These changes are published only under /movement/; preserve them when integrating remote main.

Latest user scope: work on pure movement in 3D space, without visible hands or an avatar. User confirmed push toward camera = backward, pull toward body = forward; screen-left/right motion strafes left/right. Latest clarification compares it to VR pinch-and-drag, so pinch is the default clutch, with index-extended available as an alternate. User wants very quick response.

Implementation owned by Codex: docs/movement/ only. The main docs/index.html and Claude's rig/texture files are untouched. Local test URL: http://localhost:8776/movement/ (server binds loopback). User explicitly requested phone access: production files published to d14life/rps-hand main in commit 9a2b2dd via a separate clean checkout. Public URL: https://d14life.github.io/rps-hand/movement/. Reconcile remote main before pushing hand changes; no force push. Local movement files may still appear untracked until reconciled with the remote commit. Do not remove them.

- controller.mjs: pure motion estimator/controller, pinch hysteresis, 40 ms engagement, 18 ms low-pass time constant, spatial dead zones, no inertia, relative drag travel, gap/jump rejection, release/reacquisition reanchors. Default gain 12, travel cap 12 units/sec. Rendering is independent of worker inference.
- tracker.mjs: MediaPipe 1.0.1 GPU worker, CPU initialization fallback, module WASM loader enabled. Uses image + world landmarks to estimate palm distance; hand-centred world z alone cannot measure camera translation.
- app.mjs and index.html: simple first-person reference grid, start/stop camera, sensitivity, reverse depth, gesture mode, reset, camera-free demo through the same controller. One hand only; ambiguous two-hand frames pause motion. Visibility change stops camera.
- controller.test.mjs: nine passing tests (directions, jitter, release/loss, gaps/jumps, index mode, pinch hysteresis, frame-rate consistency, invalid input).
- tracker-check.html: developer reference-image worker smoke test. Eight frames passed; warm average approximately 18.7 ms on this desktop. This is not camera-to-photon latency or a phone benchmark.

Browser demo verified right drag = positive X (1.68 m), pull = negative Z (-2.05 m). Real pinch comfort/depth noise under fast hand motion still requires user camera testing. Ordinary monocular depth is an estimate and palm rotation can disturb it; do not claim controller-grade precision without measurement.

Prior hand-review notes remain in CODEX_HAND_REVIEW.md. User has now explicitly assigned movement too, superseding that file's earlier 'navigation is not assigned' scope note.

## Movement 04 — 2026-09-10
User requested discrete gestures, not continuous index following. Default now combines one-shot horizontal turns and push/back pull/forward steps. Fold index or release pinch for 60 ms to rearm. Missing tracking never rearms a consumed gesture. Two detections confirm a stroke; a 240 ms motion window rejects slow drift. Depth uses projected palm size and world orientation via measureHand, not hand-relative world Z. Added N blue gate, E gold steps, S violet tower, W red blocks and distance markers. 16 controller tests pass; browser forward and turn demo verified. Real camera/phone latency remains unverified. Published from clean clone commit be48096, URL /movement/?v=4. Preserve docs/movement during your hand work.

Claude review: latest 16:31-16:32 plain-material renders improve materially: fist curls and V folds ring/pinky. Crossed pose cropped at tips, not enough evidence to certify crossing. Claude is actively fixing metacarpal-derived bend axis and re-rendering. No new corrective prompt while this work is on track. Do not send older correction prompt claiming all fingers remain extended; that describes superseded renders.

## Movement 05 — current user direction
Supersedes build 04. Index controls planar translation only, including simultaneous lateral/depth diagonals. No hand yaw. Head orientation belongs to separate head tracking; this isolated prototype does not add a head tracker. Movement follows displacement, not joystick offset, so holding still stops. Strict activation: index chain straightness > .96, other three fingers curled, thumb separated from index tip (pinch excluded), 70 ms activation. Opening or relaxing immediately releases; loss/outliers reanchor. Forward/back remain push=back, pull=forward. Six focused tests pass including pose exclusion, diagonals, hold still, release and missing tracking. Browser demo back-left moved X -1.0/Z +1.5 while heading stayed 0 degrees. Commit 51fead7 at /movement/?v=5. Phone accuracy not yet verified. Preserve docs/movement.

## Movement 06 — current direction
User tested v4 and reported no motion, lag, flickering detection; explicitly requested simple index-out live displacement. Supersedes strict pose from v5: other fingers/thumb ignored, extension entry .88/retain .76, no activation dwell. Missing depth does not disable lateral displacement. One hand tracker, 384 px bitmap input, render pixel ratio 1 without antialias, visible inference and frame-age ms. Stale threshold 500 ms; no extrapolation. Seven unit checks pass; actual worker passed eight reference frames at 384px with index extension .938 (desktop mean 10.7ms, not phone benchmark). Demo forward-right X+1.3/Z-1.3, heading 0. New URL /movement/?v=6. Phone responsiveness unverified.

## Movement 07
User explicitly requires fully extended index again; other fingers remain unrestricted. Check both index joint straightness and chain extension; entry .98, hold .97 for slight tracking noise. Bent finger immediately releases and reanchors. Seven controller checks pass. /movement/?v=7. Supersedes v6 permissive index gate.

## Movement 08
Default sensitivity doubled 12->24 and speed cap 10->20 m/s; range now 2–48. User asked how to avoid reverse motion when returning hand: use existing index release before return, re-extend after reposition. UI now explains this and folded status tells user to return hand then straighten. No automatic return detection: deliberate backward movement remains available while index is straight. Seven existing checks pass. /movement/?v=8, commit 6a7c14b.

## Movement 09
User requested swapping forward/back controls. Default now push toward camera = forward; pull toward body = backward. Swapped depth sign, demo inputs and labels. Sideways, strength, index gate unchanged. Seven checks pass. /movement/?v=9 commit 0f37084.

## Movement 10 — supersedes strict index requirement
User confirmed v8 always MOVE OFF and then explicitly said index just out, natural bend/wobble allowed. Reproduced strict gate rejecting actual V reference (0.871 vs required .98). Replaced joint perfection score with chain reach (.88 entry/.80 retain), other fingers ignored. Actual tracker reference now .938, controller active=true, positive translation. Seven unit checks pass. Closed-fist reference not detected by standalone tracker, so it does not validate pose rejection; synthetic curled-index test does. Diagnostics now show index score percentage. Retains v9 swapped depth: push camera=forward, pull body=back. Strong gain24. /movement/?v=10 commit f3ab9f1. Phone results not yet verified.

## Movement 11
User requested left-hand-only movement. Worker returns handedness and tracks up to two hands; selector takes one confident physical left only (raw Right because unmirrored pixels, per MediaPipe handedness convention). Ambiguous/missing/right-only labels stop and reanchor. Preview skeleton only on selected left. Relaxed index gate unchanged. Eight checks pass. /movement/?v=11. Actual phone handedness still needs verification.

## Movement 12
User requested existing repository head movement added to prototype. Reuses ../head/HeadView.js unchanged with default mode first/sensitivity1.5, yaw/pitch only; prototype forward is -Z so no full.html pi rotation. Left-index movement remains planar relative to current look yaw. Start camera loads head worker; controls Center head, head toggle and sensitivity. Face loss holds last view. Hand and face inference alternate to avoid simultaneous GPU jobs. Stop closes both workers and camera. Nine unit checks pass. Real HeadView+worker reference woman_hands.jpg detected face across8frames GPU (desktop sample latency18ms, not phone benchmark). /movement/?v=12. Existing docs/head modules identical to published repository ignoring line endings.

## Movement 13 — Dust II map
User supplied de_dust2-cs-map.zip and asked to use it as map. Extracted inner OBJ/MTL and outer34 PNG textures into docs/movement/dust2 (2.1MB), adjusted MTL texture suffix to PNG. Map loader uses existing Three OBJ/MTL loaders, rotates Z-up to Y-up and scales .0254. Textured basic materials for mobile, no new shadows. Spawn (-38,4.9012,20), ground eye offset1.65. Planar movement goes through basic substep wall raycasts and floor following, permits low steps and prevents walking off unsupported edges. This is basic walking collision, no jumping/full game physics. Existing head look and left index retained. Browser renders inspected; real geometry test walked from Z20 to wall at Z4 and remained grounded; demo forward moved Z20->16.5. Nine existing tests pass. Public /movement/?v=13. Phone performance not measured.

## Movement 14 — head joystick
Explicit user approval for continuous360 head yaw. HeadLook integrates angular rate from head yaw relative to neutral: 3degree deadzone, ramp to full rate at20degree, exponent1.4, default180deg/s atfulldeflection (Turn speed slider .5–4). Center stops immediately atnewheading; headpitch remains absolute filtered angle. Raw yaw used for rate to avoid easing drift. Face result missing/older250ms stops rotation. Center head resets neutral without changing world heading. Reset position resets heading too. Left index walking stays relative to current heading. Demo Turn left/right/Stop turning buttons added for camera-free QA. 13checks pass, includes fullrevolution, centerhold, lostface, framerate invariance. DustII browser stop held266degrees across checks. /movement/?v=14 commit361aa05. Phone performance still unverified.

## Movement 15 — automatic stairs
User requested walking stairs/half-jump obstacles without a jump gesture. Added .55m automatic step clearance, probes ahead before wall tests to avoid blocking at riser, body clearance at raised level plus ceiling tests. Normal floors follow height and descent cap1.5m remains. Synthetic browser test climbs two .5m steps, blocks tall wall and low ceiling; actual DustII spawn-to-wall regression remains grounded. /movement/?v=15. No ballistic jumping or gap jumping; this is automatic stair/small ledge stepping.

## Movement 16
User reported 360 turning too strong from small head motions. Neutral zone widened3->12degrees, adjustable6–22 via Turn threshold. Full deflection30degrees, max default speed180->90deg/s, exponent1.6 for gentler onset. Center stops instantly; no hold timer added. Five head-look tests pass including <12degrees staysstill, adjustablethreshold and continuous360. /movement/?v=16.

Build 17 (e98b7d5) published from isolated checkout C:\Users\D1\Documents\New project\movement-publish-20260910. Shared docs/movement changed concurrently during edits and had UU conflicts; do not overwrite published movement with build16. Pull latest d14life main when your merge is resolved. Build17 separates head look/body yaw, body-relative walking, hold-to-turn dwell/hysteresis, optional quick outward flick with neutral rearm, hand-priority inference scheduling and mesh bounding boxes. 16 tests pass. Local isolated preview http://localhost:8777/movement/?v=17. Public /movement/?v=17. No shared head or hand rig files changed.

Build18 7267d72 published from isolated movement-publish-20260910 checkout. Default screen thumbstick (no camera needed), held-pose calibration and legacy index strokes optional via Movement selector. Camera head tracking preserved; no hand inference in touch mode. 23 tests pass; phone layout checked. Shared movement checkout was not overwritten. Use latest d14life/main movement files, not older shared build17/16 copies.

Build19 da321df: camera thumb poses now default (left hand), thumb-weighted pose features, visible four-pose calibration and thumb tracking preview, saved templates key movement-thumb-poses-v2. Screen thumbstick and index strokes optional. Published after rebasing latest Claude commit ae34c70 without conflicts. Isolated local preview localhost:8777/movement/?v=19. No shared docs/movement files overwritten. Camera recognition needs user phone calibration; tests verify movement/stop and normalized pose features.

Build20 b9d1061: user's four JPG thumb photos processed locally with actual game tracker at384 and480 widths. All4 map correctly; .6 handedness threshold for thumb mode avoids rejecting reference3 (score .681 at480). Shipped numeric landmark references only; original photos and calibrate-photos.html remain LOCAL/untracked. Default starts ready with photo templates, manual setup optional. Tests24 pass. Public ?v=20. No shared movement source overwritten.

Build26 4679f00: user confirmed lateral controls correct; lateral thumb-index x offset, centre and gain preserved. Forward when thumb IP straight, reverse when partly curled, wrapped thumb over middle/ring fingers is rest (anatomical coverage and proximity). Open hand and Rest/Resume also stop. No photo matching. 15 relevant tests pass; live partial-curl vs tucked-hand distinction remains user phone validation. Public movement/?v=26. Shared movement source not overwritten; development remains isolated movement-publish-20260910, localhost8777.


## Current movement27 — supersedes earlier controller instructions
Public /movement/?v=27, commit5315c28. User requested v23 lateral steering restored and removal of index-knuckle connection. Restored exact thumb-base(2) to tip(4) lateral measurement, existing gain/deadzone; removed cyan index line; sideways takes priority over depth, so straight thumb does not force diagonal walking. Straight=forward, partial curl=backward, wrapped thumb=rest; Rest/Resume and head/body separation preserved. No photo matching/calibration. Screen joystick and index optional.17 tests pass. User phone drift reports motivated this change; do not claim phone validation. Latest automatic review in CODEX_HAND_REVIEW.md covers handbuild30. Handoff injection deferred due active user exchange in Claude; no user draft touched.
