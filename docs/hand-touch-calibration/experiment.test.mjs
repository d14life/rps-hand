import assert from 'node:assert/strict';import {fitContact} from './experiment.mjs';const known={L:.11,R:.13},samples=[];for(let i=0;i<50;i++){const A=[1+i*.1,.5,2+i*.02],B=[-.3,-1-i*.05,-2],c=A.map((v,k)=>v*known.L+B[k]*known.R);samples.push({A,B,c});}const fitted=fitContact(samples,known);assert.ok(Math.abs(fitted.L-known.L)<1e-10&&Math.abs(fitted.R-known.R)<1e-10);assert.equal(fitContact([],known),null);console.log('PASS: touching-hand fit recovers known scale and rejects empty input');