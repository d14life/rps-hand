import test from 'node:test';import assert from 'node:assert/strict';import {ThumbJoystick,measureThumb,fitThumbCalibration} from './thumb-joystick.mjs';
// A biased, coupled tracker: neither raw horizontal nor bend is a movement axis.
const sample=(x=0,z=0)=>({axes:[.65+.4*x+.15*z,-.3+.12*x+.5*z,.2-.2*x+.3*z,.93+.03*x-.08*z]});
const setup=()=>{const c=new ThumbJoystick();c.configure([sample(),sample(-1),sample(1),sample(0,-1),sample(0,1),{axes:[-.2,.6,.8,.1]}]);c.receive(sample(),0);return c;};
test('no movement before setup, even with the old always-forward bend',()=>{const c=new ThumbJoystick();c.receive(sample(1,-1),10);assert.deepEqual(c.step(20,.02),{dx:0,dz:0});});
test('biased centre is stationary and all four directions are reachable without cross-axis drift',()=>{const c=setup();assert.equal(c.direction,null);for(const [x,z] of [[-1,0],[1,0],[0,-1],[0,1]]){c.receive(sample(x,z),10);assert.ok(Math.abs(c.x-x)<.02);assert.ok(Math.abs(c.z-z)<.02);}});
test('backward diagonals work and diagonal speed is capped',()=>{const c=setup();for(const x of [-1,1]){c.receive(sample(x,1),20);assert.equal(Math.sign(c.x),x);assert.ok(c.z>.5);assert.ok(Math.hypot(c.x,c.z)<=1.00001);}});
test('tracking loss preserves calibration and requires centre before resuming',()=>{const c=setup(),cal=c.calibration;c.receive(sample(-1),10);c.receive(null,20);assert.equal(c.calibration,cal);c.receive(sample(1),30);assert.equal(c.direction,null);c.receive(sample(),40);c.receive(sample(-1),50);assert.ok(c.x<-.9);assert.deepEqual(c.step(401,.02),{dx:0,dz:0});assert.equal(c.calibration,cal);});
test('setup does not walk in the final backward position until centred',()=>{const c=setup();c.reset();c.receive(sample(0,1),1);assert.equal(c.direction,null);c.receive(sample(),2);c.receive(sample(0,1),3);assert.ok(c.z>.9);});
test('indistinguishable directions reject calibration instead of guessing',()=>{assert.throws(()=>fitThumbCalibration([sample(),sample(1),sample(1),sample(1),sample(1)]));assert.throws(()=>fitThumbCalibration(Array(5).fill(sample())));});
test('invalid landmarks stop',()=>assert.equal(measureThumb([],[]),null));

test('resting fist overrides movement, holds still and requires centre to resume',()=>{const c=setup();c.receive(sample(1),10);assert.ok(c.x>0);c.receive({axes:[-.2,.6,.8,.1]},20);assert.equal(c.reason,'FIST REST');assert.deepEqual(c.step(30,.02),{dx:0,dz:0});c.receive(sample(-1),40);assert.equal(c.direction,null);c.receive(sample(),50);c.receive(sample(-1),60);assert.ok(c.x<0);});
test('rest cannot be taught as the same gesture as backward',()=>assert.throws(()=>fitThumbCalibration([sample(),sample(-1),sample(1),sample(0,-1),sample(0,1),sample(0,1)]),/Resting fist/));

test('small cross-axis error cannot add forward motion to a right command',()=>{const c=setup();c.receive(sample(1,-.35),20);assert.ok(c.x>.9);assert.equal(c.z,0);c.receive(sample(1,-1),40);assert.ok(c.x>.5&&c.z<-.5);});
test('small but repeatable thumb travel calibrates without the old fixed range rejection',()=>{const small=(x=0,z=0)=>({axes:sample(x,z).axes.map((v,i)=>sample().axes[i]+(v-sample().axes[i])*.05),noise:[.00005,.00005,.00005,.00005]});const c=new ThumbJoystick();c.configure([small(),small(-1),small(1),small(0,-1),small(0,1),{axes:[-.2,.6,.8,.1]}]);c.receive(small(),0);for(const [x,z] of [[-1,0],[1,0],[0,-1],[0,1]]){c.receive(small(x,z),10);assert.ok(Math.abs(c.x-x)<.02);assert.ok(Math.abs(c.z-z)<.02);}});
test('a duplicate capture identifies only the position to retry',()=>{const captures=[sample(),sample(-1),sample(),sample(0,-1),sample(0,1),{axes:[-.2,.6,.8,.1]}];const before=JSON.stringify(captures);assert.throws(()=>fitThumbCalibration(captures),e=>e.retryIndex===2&&e.message.includes('right'));assert.equal(JSON.stringify(captures),before);captures[2]=sample(1);assert.ok(fitThumbCalibration(captures));});
test('motion hidden by capture noise is rejected even after range normalization',()=>{const captures=[sample(),sample(-.02),sample(1),sample(0,-1),sample(0,1),{axes:[-.2,.6,.8,.1]}];captures[1].noise=[.02,.02,.02,.02];assert.throws(()=>fitThumbCalibration(captures),e=>e.retryIndex===1);});
test('wrapped fist overrides even uncalibrated input and an active walking command',()=>{const c=setup();c.receive(sample(1),10);c.receive({...sample(1),rest:true},20);assert.equal(c.reason,'FIST REST');assert.deepEqual(c.step(30,.02),{dx:0,dz:0});const fresh=new ThumbJoystick();fresh.receive({...sample(1),rest:true},10);assert.equal(fresh.reason,'FIST REST');});
