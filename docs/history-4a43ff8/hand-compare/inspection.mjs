import * as T from 'three';
export function installInspection({controls,getCamera,reset,points}){
 const bar=document.createElement('section'),state={active:false};bar.innerHTML='<h2>3D viewport</h2><p>View the same tracked hand from the mirror, side, or any angle. Drag to orbit in 3D.</p>';document.querySelector('#sweep-camera').prepend(bar);const first=document.querySelector('input[aria-label="First-person view"]');const leaveFirst=()=>{if(first?.checked){first.checked=false;first.dispatchEvent(new Event('input',{bubbles:true}));}};const help=document.createElement('span');help.textContent='Drag to rotate · right-drag to pan · scroll/pinch to zoom';help.hidden=true;
 const center=()=>{const p=points();return p.length?new T.Box3().setFromPoints(p).getCenter(new T.Vector3()):new T.Vector3(0,0,-.5);};
 function inspect(side=false){leaveFirst();state.active=true;const camera=getCamera(),target=center();controls.enabled=true;controls.enableDamping=false;controls.enableRotate=true;controls.enablePan=true;controls.enableZoom=true;controls.minDistance=.03;controls.maxDistance=10;controls.minPolarAngle=0;controls.maxPolarAngle=Math.PI;controls.target.copy(target);if(side)camera.position.copy(target).add(new T.Vector3(.45,0,0));else camera.position.copy(target).add(new T.Vector3(.18,.1,.45));camera.up.set(0,1,0);camera.lookAt(target);camera.updateProjectionMatrix();controls.update();document.querySelector('#scene').style.transform='none';document.querySelector('#cameraFrame').style.display='none';help.hidden=false;}
 function mirror(){leaveFirst();state.active=false;controls.enabled=false;help.hidden=true;reset();}
 for(const [name,fn]of [['Mirror view',mirror],['3D view — rotate',()=>inspect()],['Side view',()=>inspect(true)]]){const b=document.createElement('button');b.textContent=name;b.onclick=fn;bar.append(b);}bar.append(help);
 first?.addEventListener('input',()=>{if(first.checked){state.active=false;controls.enabled=false;help.hidden=true;}});document.querySelector('#viewReset').onclick=mirror;return state;
}
